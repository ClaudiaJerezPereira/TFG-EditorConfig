[project]
name = "editor"
version = "2.0.0"
description = "Editor gráfico de los controles de arbitraje de Eurobot"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pillow>=12.3.0",
]
